# Twitch Interactive Bot — Python dependencies
# Install with:  pip install -r requirements.txt

# GUI framework
customtkinter>=5.2.0

# HTTP client for local LLM (Ollama / LM Studio)
requests>=2.31.0

# Audio playback (TTS output)
pygame>=2.5.0

# Game input simulation
# pydirectinput works on Windows (DirectX / Raw-input compatible)
# pynput works on Linux, macOS, and Windows as a fallback
pydirectinput>=1.0.4
pynput>=1.7.6
