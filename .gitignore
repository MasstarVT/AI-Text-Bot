# Credentials — never commit
.env

# Python virtual environment
.venv/

# Bundled Piper TTS binary (25 MB — downloaded separately)
piper/
*.wav

# Voice model binaries (large — only commit the .json config)
Voices/*.onnx

# Byte-compiled files
__pycache__/
*.pyc
